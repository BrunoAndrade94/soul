// const fs = require("fs");
// const csv = require("fast-csv");
// const stream = fs.createReadStream("R_PRODUTO.csv");

// // const streamCsv = csv({}).on("data", (d) => console.log(d));

// stream.pipe(streamCsv);
